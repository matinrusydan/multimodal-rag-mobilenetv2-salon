import { LoginRequestSchema, RegisterRequestSchema } from '@rag-salon/shared-types';

export const registerSchema = { body: RegisterRequestSchema };
export const loginSchema = { body: LoginRequestSchema };
