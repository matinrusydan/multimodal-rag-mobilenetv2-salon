# FastAPI brain engine (apps/ai) — runtime AI: CV, RAG (ChromaDB + Gemini), Crawl4AI.
# Install: python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
python-dotenv>=1.0

# CV inference
onnxruntime>=1.18
Pillow>=10.0
numpy>=2.0

# RAG — chromadb native + Gemini
chromadb>=0.5
google-genai>=1.0

# Crawling (Crawl4AI, Python-only)
crawl4ai>=0.6

# Client/tooling
httpx>=0.27
pytest>=8.0
pytest-asyncio>=0.24